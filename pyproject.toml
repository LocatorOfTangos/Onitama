[project]
name = "onitama"
version = "0.1.1"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
onitama = "onitama.main:main"
